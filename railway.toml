# Railway deploy config (europe-west4). Root Directory must be this folder.

[deploy]
startCommand = "/app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
